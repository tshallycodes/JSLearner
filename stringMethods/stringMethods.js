// string methods = allow you to manipulate and work with text (strings)

let userName = "Bro Code";
let phoneNumber = "123-456-7890";

//console.log(userName.length); // length of value
//console.log(userName.charAt(0)); // shows character at the index specificied
//console.log(userName.indexOf("o")); // shows index of the first occurrence of a chracter
//console.log(userName.lastIndexOf("o")); //last index of a specific character
//userName = userName.trim(); // used to remove whitespace in a string 
//userName = userName.toUpperCase(); // change to uppercase
//userName = userName.toLowerCase(); // change to lower case
//userName = userName.repeat(3); 
//let result = userName.startsWith(" ");  // check if a string starts with a char
//let result = userName.endsWith(" "); // check if a string ends with a char
//let result = userName.includes(" "); // checks if it has a char or symbol
//phoneNumber = phoneNumber.replaceAll("-", ""); // replace a char with another char
//phoneNumber = phoneNumber.padStart(15, "0"); // Adds the second value assigned in the argument in front of the value of the variable until the length of the value is equal to the specified number
//phoneNumber = phoneNumber.padEnd(15, "0"); // adds at the back

console.log(phoneNumber);